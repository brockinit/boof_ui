service: boof-test

frameworkVersion: ">=1.8.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs6.10
  environment:
    API_KEY: "FILL IN API KEY"

functions:
  mailchimp:
    handler: mailchimp/index.handler
    events:
      - http:
          path: /
          method: get
          cors: true
